# Security policy

このリポジトリは参考実装です。公開コードの例示ホストや例示パスを、そのまま実環境へ使わないでください。

## Gitへ入れないもの

- Obsidian保管庫の本文・索引・会話記録
- Google Driveの文書IDや本文
- 実機スクリーンショット、録音、PCM、WAV
- 生成AIX、一時URL、Bearer token、bootstrap token
- API key、GitHub token、Cloudflare credentials、証明書、秘密鍵、`.env`
- 実際に利用しているホスト名、個人の絶対パス

## 実行時に維持する境界

- Mac受け口は `127.0.0.1` だけで待ち受ける
- 公開経路は必要なパスだけをローカル受け口へ転送する
- 会話ごとの短命認証と、一回限りの確認を使う
- 保存・編集能力を会話AIへ直接渡さない
- 成功、失敗、取消、期限切れのすべてで一時受け口、認証、AIX、音声を後片付けする

脆弱性らしい問題を見つけた場合、秘密値や個人データを公開Issueへ貼り付けないでください。GitHubのSecurity機能から非公開で報告してください。
